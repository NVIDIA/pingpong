# Third-Party Notices

This release does not distribute vendored or copied third-party open source
code.

The benchmark builds against platform-provided system interfaces and libraries,
including Linux, glibc-compatible C library interfaces, GCC-compatible compiler
tooling, pthreads, and Linux `perf_event_open` / perf event headers. Those
components are not distributed as part of this source release.
